$(document).ready(function() {

  var contenedor = $("#contenedor");

  // Eliminar elemento y sus elementos hijo
  //contenedor.remove();

  // Eliminar los elementos hijo de un elemento
  //contenedor.children().remove();
  //contenedor.children("#segunda").remove();
  contenedor.empty();

});